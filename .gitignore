node_modules/
dist/
build/
.env
*.local
.DS_Store
coverage/
*.log
.turbo/

# Claude Code session data
.claude/

# Vercel — commit project.json (IDs only, no secrets), ignore runtime files
.vercel/*.vc-config.json
.vercel/output

# Binary design docs (large, not source code)
*.docx
