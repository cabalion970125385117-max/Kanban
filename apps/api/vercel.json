{
  "version": 2,
  "installCommand": "cd ../.. && npm install --include=dev && npm run build --workspace=packages/shared",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.ts" }
  ]
}
