{
  "installCommand": "npm install --include=dev",
  "buildCommand": "node node_modules/.bin/vite build --root apps/web",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
